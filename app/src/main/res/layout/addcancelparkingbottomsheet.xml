<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout app:layout_behavior="android.support.design.widget.BottomSheetBehavior"
    android:id="@+id/fragment_history_menu_bottom"
    android:layout_width="wrap_content"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:background="#77ffffff"
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto">

    <RelativeLayout
        android:id="@+id/bottom_sheet_add_parking"
        android:layout_width="185dp"
        android:layout_height="60dp"
        android:foreground="?android:attr/selectableItemBackground"
        android:orientation="horizontal"
        android:padding="16dp"
        android:clickable="true"
        android:focusable="true"
        android:background="@drawable/background_border">


        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginStart="16dp"
            android:text="Add parking"
            android:textColor="#000"
            android:textSize="20sp"
            android:id="@+id/bottom_title"/>

        <ImageView
            android:layout_width="50dp"
            android:layout_height="35dp"
            android:layout_toEndOf="@+id/bottom_title"
            android:src="@drawable/ic_add"
            android:id="@+id/ic_add"/>

    </RelativeLayout>

    <RelativeLayout
        android:id="@+id/bottom_sheet_cancel"
        android:layout_width="wrap_content"
        android:layout_height="60dp"
        android:orientation="horizontal"
        android:foreground="?android:attr/selectableItemBackground"
        android:padding="16dp"
        android:layout_toEndOf="@+id/bottom_sheet_add_parking"
        android:clickable="true"
        android:focusable="true"
        android:layout_alignParentEnd="true"
        android:layout_marginStart="30dp"
        android:background="@drawable/background_border">

        <TextView
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_gravity="center"
            android:layout_marginStart="16dp"
            android:textSize="20sp"
            android:textColor="#000"
            android:text="Cancel"
            android:id="@+id/cancel"/>

        <ImageView

            android:layout_width="50dp"
            android:layout_height="35dp"
            android:src="@drawable/ic_cancel"
            android:layout_toEndOf="@+id/cancel"
            android:id="@+id/ic_cancel"/>

    </RelativeLayout>
</RelativeLayout>